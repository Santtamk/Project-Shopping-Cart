

const Cart = ({ cartLength }) => {

  return (
    <div>
        {/* {cartLength.map((item) => (
            <div key={item.id}>{item.id}</div>
        ))} */}

        {cartLength}
    </div>
  )
}

export default Cart